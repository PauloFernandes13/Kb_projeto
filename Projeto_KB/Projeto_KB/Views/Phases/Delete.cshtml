@model Projeto_KB.Models.Phase

@{Layout = "~/Views/Shared/_ConceptsLayout.cshtml";}
@{ViewBag.Title = "Apagar Etapa de Implementação";}

<div class="SubjectBody">
    <h2 class="log" style="text-align:center">@ViewBag.Title</h2>
    <h3 class="new">De certeza que pretende apagar <span class="headers">@Html.DisplayFor(c => c.Name)</span> ?</h3>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="registerFeature">
            <div style="text-align:center">
                <input type="submit" value="SIM" class="btn btn-info button-small" />
                @Html.ActionLink("NÃO", "Index", "Subjects", null, new { @class = "btn btn-info button-small" })
            </div>
        </div>

    }
</div>
